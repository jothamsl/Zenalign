{
  "name": "Senalign Dataset Quality Validator",
  "description": "AI-powered dataset quality validation with GPT-4o analysis",
  "repository": "https://github.com/YOUR_USERNAME/senalign",
  "keywords": ["data-quality", "ml", "dataset-validation", "gpt-4", "fastapi", "react"],
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    },
    {
      "url": "heroku/python"
    }
  ],
  "env": {
    "OPENAI_API_KEY": {
      "description": "Your OpenAI API key for GPT-4o analysis (required)",
      "required": true
    },
    "SECRET_KEY": {
      "description": "Secret key for the application",
      "generator": "secret",
      "required": true
    },
    "PYTHON_VERSION": {
      "description": "Python version",
      "value": "3.11.6"
    }
  },
  "formation": {
    "web": {
      "quantity": 1,
      "size": "basic"
    }
  },
  "stack": "heroku-22"
}
